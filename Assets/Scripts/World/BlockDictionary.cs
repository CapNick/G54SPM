using System.Collections.Generic;
using UnityEngine;

namespace World {
    public class BlockDictionary : MonoBehaviour {
        public Dictionary<int, BlockType> BlockDict = new Dictionary<int, BlockType>();

    }
}